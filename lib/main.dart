import 'package:flutter/material.dart';
import 'package:shoop/Home.dart';

void main() => runApp(new MaterialApp(
      debugShowCheckedModeBanner:
          false, // for disapper debug mod sign in the corner
      debugShowMaterialGrid: false,
      title: "first app",
      home: Home(),
    ));
